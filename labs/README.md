# Labs

Teaching scripts land here as lectures ship. One folder per lab, parts inside it.

Clone and pytest do not need a file in this folder yet. The first typed lab is S1.3 (green clone), then the loop labs in S2.
