# Copy to .env. Never commit .env.
# Pytest is green with no keys set.

# Local default. No key. Verify the tag at record time (docs/CURRENCY.md).
OLLAMA_BASE_URL=http://localhost:11434/v1
OLLAMA_CHAT_MODEL=llama3.2:3b

# Cloud. Leave blank until a live lab. Verify ids at record time.
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
OPENAI_CHAT_MODEL=
ANTHROPIC_CHAT_MODEL=
