
# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# Virtual environment
.venv/
venv/
env/
ENV/
env.bak/
venv.bak/

# Vector store files
dataflow_vector_store/
index.faiss
index.pkl
*.faiss
*.pkl

# Jupyter Notebook checkpoints
.ipynb_checkpoints/

# VS Code settings
.vscode/

# PyCharm settings
.idea/

# VS-specific files
.vs/
*.suo
*.user
*.userosscache
*.sln.docstates

# Python environment
*.env

# Mac system files
.DS_Store

# SQLite DBs or temp files
*.db
*.db-shm
*.db-wal

# Lab 6.4.3 SQLite checkpointer. Created at run time, not committed.
dataflow/data/checkpoints.sqlite
dataflow/data/checkpoints.sqlite-wal
dataflow/data/checkpoints.sqlite-shm

# Lab 6.5 refund ledger. Created at run time, not committed.
dataflow/data/refunds.jsonl

# Lab 6.8 receipt task and sqlite checkpointer. Created at run time, not committed.
dataflow/data/receipts.jsonl
dataflow/data/checkpoints_14.sqlite
dataflow/data/checkpoints_14.sqlite-wal
dataflow/data/checkpoints_14.sqlite-shm

# Lab 6.10 local traces. Created at run time, not committed.
dataflow/data/traces/

# langgraph dev local runtime. Created at run time, not committed.
.langgraph_api/

# Lab 6.11 research agent runtime reports. Keep the sample and .gitkeep.
research_agent/reports/*
!research_agent/reports/.gitkeep
!research_agent/reports/sample_return_complaints.md

# Lab 6.12 ambient sqlite, drop folder, nightly reports. Keep .gitkeep.
dataflow/data/ambient.sqlite
dataflow/data/ambient.sqlite-wal
dataflow/data/ambient.sqlite-shm
dataflow/data/inbox_drop/*
!dataflow/data/inbox_drop/.gitkeep
dataflow/data/reports/*
!dataflow/data/reports/.gitkeep

# Part 3 notes and rot capture. Created at run time, not committed.
dataflow/context/notes.json
dataflow/context/_last_rot.json

# TechCorp password-reset audit. Created at run time, not committed.
techcorp/data/audit.jsonl

# Section 23 FAISS index. Built at run time, not committed.
dataflow/data/faiss_index/
dataflow/data/faiss_index_all/

# Project-specific IDE metadata
*.iml

# Logs
*.log

# Ignore compiled python files
*.pyo
*.pyd

# Coverage reports
htmlcov/
.tox/
.nox/
.coverage
coverage.xml
.cache
nosetests.xml
pytest_cache/
.pytest_cache/
.env
!.env.sample
